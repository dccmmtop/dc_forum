<div class="widget category-widget">
  <h4 class="title">分类</h4>
  <div class="category">
     <% @categories.each do |category|%>
       <%= link_to category.name<<"(#{category.topics_count})",category_path(category)%>
       <br>
     <% end %>
  </div>
</div>